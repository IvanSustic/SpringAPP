import { VozilaSortPipe } from './vozila-sort.pipe';

describe('VozilaSortPipe', () => {
  it('create an instance', () => {
    const pipe = new VozilaSortPipe();
    expect(pipe).toBeTruthy();
  });
});
